package it.ashyzan.ticket_platform.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import it.ashyzan.ticket_platform.model.Stato;

public interface StatoRepo extends JpaRepository<Stato, Integer>{

}
